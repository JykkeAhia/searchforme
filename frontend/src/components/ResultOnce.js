import React from 'react';

const ResultOnce = (props) => {
    // TODO get search event result but only the most new result
    return <div>Search_id: {props.search_id}</div>;
};

export default ResultOnce;